#include<stdio.h>
#include<stdlib.h>  // 提供exit()原型


int main(int argc, char * argv[])
{
	int ch;  //
	FILE * fp;  //
	unsigned long count = 0;
	if (argc != 2)
	{
		printf_s("Usage %s filename\n", argv[0]);
		exit(EXIT_FAILURE);
	}
	if ((fp = fopen(ar)))


	return 0;
}
